# 通用信息架构原则

> **职责**：Claude Code 项目信息组织的 11 项通用原则。可跨项目复用。
>
> **定位**：通用理论层。各项目可基于这些原则制定自己的具体设计原则。
>
> **按需加载**：审视信息架构、创建新模板、审计文档结构时参考。
>
> **跨文档引用**：本原则被 [design-principles.md](design-principles.md) 实现为 6 条具体设计原则。

## §0 速查卡片

| 原则 | 一句话 | 核心关键词 |
|------|--------|-----------|
| IA-1 | 信息从抽象流向具体 | 单向依赖 |
| IA-2 | 不同抽象层级分文件 | 抽象分层 |
| IA-3 | 高扇入文件隔离易变内容 | 稳定-易变分离 |
| IA-4 | 不同信息类型不混放 | 信息分类 |
| IA-5 | 只加载当前所需最小集 | 按需加载 |
| IA-6 | 每条信息一个权威定义点 | 单一权威 |
| IA-7 | 执行保障匹配关键程度 | 确定性分级 |
| IA-8 | 入口为发现而设计 | 可发现性优先 |
| IA-9 | 面向 LLM 精确无歧义 | 认知清晰 |
| IA-10 | 数据格式由独立契约约束 | 契约隔离 |
| IA-11 | 每个文件一个核心职责 | 单一职责 |

## 11 原则

| # | 原则 | 核心要求 | 应用指南 |
|---|------|---------|---------|
| IA-1 | 单向依赖 | 信息从抽象流向具体，不可反向 | 设计分层架构时，确保上层文件不依赖下层实现 |
| IA-2 | 抽象分层 | 不同抽象层级放在不同文件 | 通用原则与具体实现分文件定义，按层级组织目录 |
| IA-3 | 稳定-易变分离 | 高扇入文件隔离易变内容以保持稳定 | 将频繁变更的路径、版本号等提取到独立文件，高引用文件仅保留稳定内容 |
| IA-4 | 信息分类 | 不同信息类型不混放 | 规则、参考文档、项目文档等按类型分目录存放 |
| IA-5 | 按需加载 | 只加载当前上下文所需的最小信息集 | 将详细内容放在独立文件中，入口文件仅提供索引和链接 |
| IA-6 | 单一权威 | 每条信息有且仅有一个权威定义点 | 定义概念时选定唯一文件，其他文件通过链接引用，不复制内容 |
| IA-7 | 确定性分级 | 约束的执行保障级别匹配其关键程度 | 为约束标注级别（如铁律/必须/推荐），关键约束配备自动化检测 |
| IA-8 | 可发现性优先 | 入口信息为发现而设计，不为完整而设计 | 在文件开头放置速查卡片和索引表，帮助快速定位所需信息 |
| IA-9 | 认知清晰 | 面向 LLM 的指令精确无歧义，阻止合理化绕过 | 使用表格、矩阵等结构化格式表达复杂关系，避免模糊的散文描述 |
| IA-10 | 契约隔离 | 数据格式由独立契约层约束 | 模板变量、API 格式等定义在独立的规范文件中，与使用方解耦 |
| IA-11 | 单一职责 | 每个文件只有一个核心职责 | 在文件头部声明职责，内容超出职责范围时拆分为独立文件 |

## 本项目映射

将通用原则映射为 claude-context-templates 的具体实践和检测方法：

| # | 原则 | 项目映射 | 检测/参照 |
|---|------|---------|----------|
| IA-1 | 单向依赖 | 产品层→开发层引用禁止 | 检测命令见 `project-structure.md` §3 |
| IA-2 | 抽象分层 | rules（自动加载）→ references（按需）→ docs（深度参考） | CLAUDE.md §0 文件索引"加载方式"列 |
| IA-3 | 稳定-易变分离 | rules/ 保持稳定（高扇入），docs/ 允许频繁更新 | `git log --oneline <file> \| wc -l` 检查 rules 文件变更频率 |
| IA-4 | 信息分类 | 规范（rules）、参考（references）、流程（docs）严格分开 | 详见 `project-structure.md` §2 分层归属表 |
| IA-5 | 按需加载 | references/ 和 docs/ 不自动加载；SKILL description 保持简洁 | CLAUDE.md §0 的"加载方式"列标注 |
| IA-6 | 单一权威 | 每个概念标注 SSoT 所在文件 | 详见 `design-principles.md` 原则 1 |
| IA-7 | 确定性分级 | 分层约束有 grep 检测；CSO 有常见陷阱表；关键规则有反合理化清单 | 铁律是否有对应自动化检测或 grep 命令 |
| IA-8 | 可发现性优先 | CLAUDE.md 作为索引页，§0 速查卡片优先 | 详见 `design-principles.md` 原则 2 |
| IA-9 | 认知清晰 | 关键规则配反合理化清单（分层约束、CSO） | `project-structure.md` §3 和 `plugin-dev-spec.md` §3 |
| IA-10 | 契约隔离 | `context-schema.yaml` 独立定义 Preset 结构契约 | Schema 文件不含业务逻辑 |
| IA-11 | 单一职责 | 每个 rules 文件一个核心职责（见文件头部职责声明） | 文件首行 `> **职责**：...` |

## 约束分级标记

| 标记 | 含义 | 执行保障 |
|------|------|---------|
| `iron rule` | 铁律，不可违反 | 应有 Hook 或自动化测试 |
| `required` | 必须遵循 | 代码审查检查 |
| `recommended` | 推荐实践 | 最佳实践指引 |
